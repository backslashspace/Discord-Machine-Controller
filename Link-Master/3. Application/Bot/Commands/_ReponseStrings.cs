using System;

namespace Link_Master.Worker
{
    internal struct ResponseStrings
    {
        internal static readonly String No_Permission = "You have no permissions to execute this command.";
    }
}